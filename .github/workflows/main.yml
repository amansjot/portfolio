name: Deploy React Portfolio

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies
        run: npm ci

      - name: Set homepage and repository fields
        run: |
          npx json -I -f package.json -e 'this.homepage="https://amansingh.dev"'
          npx json -I -f package.json -e 'this.repository={ "type": "git", "url": "https://$GH_TOKEN@github.com/${{ github.repository }}.git" }'

      - name: Build the app
        run: npm run build

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Deploy to GitHub Pages
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run deploy
